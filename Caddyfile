# Global config — applies to all sites.
{
    email {$ACME_EMAIL}
}

{$TELEMETRY_DOMAIN} {
    encode zstd gzip
    reverse_proxy api:8000
}

{$GRAFANA_DOMAIN} {
    encode zstd gzip
    reverse_proxy grafana:3000
}
